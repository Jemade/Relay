apiVersion: v1
kind: Service
metadata:
  name: relay-service
  namespace: relay
  labels:
    app.kubernetes.io/name: relay
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relay
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
